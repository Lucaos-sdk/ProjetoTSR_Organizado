# Pasta legada — não é um pacote pronto para o jogo

O ONNX desta pasta referencia pesos externos ausentes. Não há OptiScaler.dll validada neste snapshot. A DLL ONNX Runtime existente não teve sua correspondência com os headers/provider certificada.

Não existe nova distribuição TSR gerada nesta revisão. Consulte o README da raiz e docs/AUDITORIA.md. O modelo aleatório criado com --allow-untrained serve apenas a testes estruturais e não deve ser usado como upscaler.
