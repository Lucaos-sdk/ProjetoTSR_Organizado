cmake_minimum_required(VERSION 3.20)
project(TSRBaseline LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
enable_testing()
add_executable(tsr_camera_history_tests camera_history_tests.cpp)
add_test(NAME camera_history COMMAND tsr_camera_history_tests)
add_executable(tsr_camera_contract_tests camera_contract_tests.cpp)
add_test(NAME camera_contract COMMAND tsr_camera_contract_tests)
add_executable(tsr_frame_generation_policy_tests frame_generation_policy_tests.cpp)
add_test(NAME frame_generation_policy COMMAND tsr_frame_generation_policy_tests)
add_executable(tsr_frame_slots_tests frame_slots_tests.cpp)
add_test(NAME frame_slots COMMAND tsr_frame_slots_tests)
add_executable(tsr_submission_observer_tests submission_observer_tests.cpp)
add_test(NAME submission_observer COMMAND tsr_submission_observer_tests)
add_executable(tsr_texture_region_tests texture_region_tests.cpp)
add_test(NAME texture_regions COMMAND tsr_texture_region_tests)
add_executable(tsr_reference_tests reference_tests.cpp)
add_test(NAME reference COMMAND tsr_reference_tests)
add_executable(tsr_frame_contract_tests frame_contract_tests.cpp)
add_test(NAME frame_contract COMMAND tsr_frame_contract_tests)
add_executable(tsr_temporal_reference_tests temporal_reference_tests.cpp)
add_test(NAME temporal_reference COMMAND tsr_temporal_reference_tests)
add_executable(tsr_reconstruction_tests reconstruction_tests.cpp)
add_test(NAME reconstruction COMMAND tsr_reconstruction_tests)
add_executable(tsr_output_history_tests output_history_tests.cpp)
add_test(NAME output_history_reference COMMAND tsr_output_history_tests)
add_executable(tsr_runtime_options_tests runtime_options_tests.cpp)
add_test(NAME runtime_options COMMAND tsr_runtime_options_tests)
if(WIN32)
  add_executable(tsr_relighting_world_dx12 relighting_world_dx12.cpp)
  target_include_directories(tsr_relighting_world_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_relighting_world_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_relighting_world_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME relighting_world_warp COMMAND tsr_relighting_world_dx12 --warp)
  set_tests_properties(relighting_world_warp PROPERTIES TIMEOUT 120)
  add_executable(tsr_game_relighting_dx12 game_relighting_dx12.cpp)
  target_include_directories(tsr_game_relighting_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_game_relighting_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_game_relighting_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME game_relighting_warp COMMAND tsr_game_relighting_dx12 --warp)
  set_tests_properties(game_relighting_warp PROPERTIES TIMEOUT 120)
  # OptiScaler's local Detours library is not distributed with the baseline CI.
  set(TSR_DETOURS_LIB "${CMAKE_CURRENT_SOURCE_DIR}/../../_OptiScaler_Source/OptiScaler/library/detours/detours.lib")
  if(EXISTS "${TSR_DETOURS_LIB}")
    add_executable(tsr_game_relighting_hooks_dx12 game_relighting_dx12.cpp)
    target_include_directories(tsr_game_relighting_hooks_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}"
      "${CMAKE_CURRENT_SOURCE_DIR}/../../_OptiScaler_Source/OptiScaler/include")
    target_compile_definitions(tsr_game_relighting_hooks_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN TSR_TEST_SUBMISSION_HOOKS)
    target_link_libraries(tsr_game_relighting_hooks_dx12 PRIVATE d3d12 dxgi d3dcompiler "${TSR_DETOURS_LIB}")
    add_test(NAME game_relighting_hooks_warp COMMAND tsr_game_relighting_hooks_dx12 --warp)
    set_tests_properties(game_relighting_hooks_warp PROPERTIES TIMEOUT 120)
  endif()
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Relighting.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/Relighting.hlsl" RELIGHTING_SOURCE)
  configure_file(relighting_shader.h.in relighting_shader.h @ONLY)
  add_executable(tsr_relighting_dx12 relighting_dx12.cpp)
  target_include_directories(tsr_relighting_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_relighting_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_relighting_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_executable(tsr_shader_texture_desc_dx12 shader_texture_desc_dx12.cpp)
  target_include_directories(tsr_shader_texture_desc_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_shader_texture_desc_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_shader_texture_desc_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME shader_texture_desc_warp COMMAND tsr_shader_texture_desc_dx12 --warp)
  add_executable(tsr_framegen_capabilities_dx12 framegen_capabilities_dx12.cpp)
  target_include_directories(tsr_framegen_capabilities_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_framegen_capabilities_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_framegen_capabilities_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_executable(tsr_fsr_sdk_dx12 fsr_sdk_dx12.cpp)
  target_include_directories(tsr_fsr_sdk_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_fsr_sdk_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_fsr_sdk_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_executable(tsr_fsr_legacy_dx12 fsr_sdk_dx12.cpp)
  target_include_directories(tsr_fsr_legacy_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_fsr_legacy_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN TSR_FSR_LEGACY)
  target_link_libraries(tsr_fsr_legacy_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  set(TSR_NGX_INCLUDE "${CMAKE_CURRENT_SOURCE_DIR}/../../_OptiScaler_Source/external/nvngx_dlss_sdk")
  add_executable(tsr_ngx_frame_adapter_tests ngx_frame_adapter_tests.cpp)
  target_include_directories(tsr_ngx_frame_adapter_tests PRIVATE "${TSR_NGX_INCLUDE}")
  add_test(NAME ngx_frame_adapter COMMAND tsr_ngx_frame_adapter_tests)
  add_executable(tsr_command_list_lifetime_dx12 command_list_lifetime_dx12.cpp)
  target_include_directories(tsr_command_list_lifetime_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_command_list_lifetime_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_command_list_lifetime_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME command_list_lifetime_warp COMMAND tsr_command_list_lifetime_dx12 --warp)
  set_tests_properties(command_list_lifetime_warp PROPERTIES TIMEOUT 120)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Bilinear.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/Bilinear.hlsl" BILINEAR_SOURCE)
  configure_file(shader_source.h.in shader_source.h @ONLY)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/Temporal.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/Temporal.hlsl" TEMPORAL_SOURCE)
  configure_file(temporal_shader.h.in temporal_shader.h @ONLY)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/OutputHistory.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/OutputHistory.hlsl" OUTPUT_HISTORY_SOURCE)
  configure_file(output_history_shader.h.in output_history_shader.h @ONLY)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/InputAdapter.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/InputAdapter.hlsl" INPUT_ADAPTER_SOURCE)
  configure_file(input_adapter_shader.h.in input_adapter_shader.h @ONLY)
  add_executable(tsr_input_adapter_dx12 input_adapter_dx12.cpp)
  target_include_directories(tsr_input_adapter_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_input_adapter_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_input_adapter_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME input_adapter_warp COMMAND tsr_input_adapter_dx12 --warp)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/OutputAdapter.hlsl")
  file(READ "${CMAKE_CURRENT_SOURCE_DIR}/OutputAdapter.hlsl" OUTPUT_ADAPTER_SOURCE)
  configure_file(output_adapter_shader.h.in output_adapter_shader.h @ONLY)
  add_executable(tsr_input_chain_dx12 input_chain_dx12.cpp)
  target_include_directories(tsr_input_chain_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_input_chain_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_input_chain_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME input_chain_warp COMMAND tsr_input_chain_dx12 --warp)
  add_test(NAME input_chain_fp16_warp COMMAND tsr_input_chain_dx12 --warp --fp16)
  add_test(NAME input_regions_warp COMMAND tsr_input_chain_dx12 --warp --regions)
  add_test(NAME input_regions_fp16_warp COMMAND tsr_input_chain_dx12 --warp --regions --fp16)

  add_executable(tsr_gpu_context_dx12 gpu_context_dx12.cpp)
  target_include_directories(tsr_gpu_context_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_include_directories(tsr_gpu_context_dx12 PRIVATE "${TSR_NGX_INCLUDE}")
  target_compile_definitions(tsr_gpu_context_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_gpu_context_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME gpu_context_warp COMMAND tsr_gpu_context_dx12 --warp --debug)
  add_test(NAME gpu_context_fp16_warp COMMAND tsr_gpu_context_dx12 --warp --debug --fp16)
  add_test(NAME ngx_current_frame_warp COMMAND tsr_gpu_context_dx12 --warp --debug --current-frame-only)
  add_test(NAME gpu_stage_timing_warp COMMAND tsr_gpu_context_dx12 --warp --debug --fp16 --benchmark gpu-stage-timing.csv)
  set_tests_properties(gpu_stage_timing_warp PROPERTIES TIMEOUT 120)
  set_tests_properties(gpu_context_warp gpu_context_fp16_warp PROPERTIES TIMEOUT 120)
  add_executable(tsr_frame_slots_dx12 frame_slots_dx12.cpp)
  target_include_directories(tsr_frame_slots_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_frame_slots_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_frame_slots_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  add_test(NAME frame_slots_warp COMMAND tsr_frame_slots_dx12 --warp)
  set_tests_properties(frame_slots_warp PROPERTIES TIMEOUT 120)
  add_executable(tsr_temporal_dx12 temporal_dx12.cpp)
  target_include_directories(tsr_temporal_dx12 PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_temporal_dx12 PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_temporal_dx12 PRIVATE d3d12 dxgi d3dcompiler)
  if(MSVC)
    target_compile_options(tsr_temporal_dx12 PRIVATE /W4 /permissive-)
  endif()
  add_test(NAME benchmark_output_history_warp COMMAND tsr_temporal_dx12 --warp --output-history --render 16x9 --output 24x14 --benchmark benchmark-output.csv --warmup 2 --samples 3)
  add_test(NAME benchmark_bilinear_warp COMMAND tsr_temporal_dx12 --warp --render 16x9 --output 24x14 --benchmark benchmark-bilinear.csv --warmup 2 --samples 3)
  add_test(NAME temporal_warp COMMAND tsr_temporal_dx12 --warp)
  add_test(NAME temporal_warp_full COMMAND tsr_temporal_dx12 --warp --full)
  add_test(NAME temporal_cubic_warp COMMAND tsr_temporal_dx12 --warp --cubic)
  add_test(NAME temporal_cubic_warp_full COMMAND tsr_temporal_dx12 --warp --cubic --full)
  add_test(NAME output_history_warp COMMAND tsr_temporal_dx12 --warp --output-history)
  add_test(NAME output_history_warp_full COMMAND tsr_temporal_dx12 --warp --output-history --full)
  set_tests_properties(output_history_warp output_history_warp_full PROPERTIES TIMEOUT 120)
  add_test(NAME output_history_1080p_warp COMMAND tsr_temporal_dx12 --warp --output-history --1080p)
  set_tests_properties(output_history_1080p_warp PROPERTIES TIMEOUT 120)
  add_test(NAME output_history_fractional_wide COMMAND tsr_temporal_dx12 --warp --output-history --render 1706x2 --output 2560x3)
  set_tests_properties(output_history_fractional_wide PROPERTIES TIMEOUT 120)
  set_tests_properties(temporal_cubic_warp temporal_cubic_warp_full PROPERTIES TIMEOUT 120)
  set_tests_properties(temporal_warp temporal_warp_full PROPERTIES TIMEOUT 120)
  add_executable(tsr_dx12_baseline dx12_baseline.cpp)
  target_include_directories(tsr_dx12_baseline PRIVATE "${CMAKE_CURRENT_BINARY_DIR}")
  target_compile_definitions(tsr_dx12_baseline PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
  target_link_libraries(tsr_dx12_baseline PRIVATE d3d12 dxgi d3dcompiler)
  if(MSVC)
    target_compile_options(tsr_dx12_baseline PRIVATE /W4 /permissive-)
  endif()
  add_test(NAME dx12_warp COMMAND tsr_dx12_baseline --warp)
  add_test(NAME dx12_warp_full COMMAND tsr_dx12_baseline --warp --full)
  add_test(NAME dx12_texture_warp COMMAND tsr_dx12_baseline --warp --textures)
  add_test(NAME dx12_texture_warp_full COMMAND tsr_dx12_baseline --warp --textures --full)
  set_tests_properties(dx12_texture_warp dx12_texture_warp_full PROPERTIES TIMEOUT 120)
  set_tests_properties(dx12_warp PROPERTIES TIMEOUT 120)
  set_tests_properties(dx12_warp_full PROPERTIES TIMEOUT 120)
endif()
